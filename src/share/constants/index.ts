import { AuthConstants } from './auth.constant';

export class AppConstants {
  static readonly Auth = AuthConstants;
  static readonly PAGE_DEFAULT: number = 1;
  static readonly LIMIT_DEFAULT: number = 10;
}
